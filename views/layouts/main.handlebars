<html lang='en'>

  <head>
    <meta charset='UTF-8' />
    <meta name='viewport' content='width=device-width, initial-scale=1.0' />
    <title>Bookbusta</title>
    <script
      src='https://kit.fontawesome.com/0ba112b42d.js'
      crossorigin='anonymous'
    ></script>
    <link
      href='https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css'
      rel='stylesheet'
      integrity='sha384-9ndCyUaIbzAi2FUVXJi0CjmCapSmO7SnpJef0486qhLnuZ2cdeRhO02iuK6FUUVM'
      crossorigin='anonymous'
    />
    <link
      rel='stylesheet'
      href='https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css'
    />
    <link rel='stylesheet' href='/css/jass.css' />
    <link rel='stylesheet' href='/css/style.css' />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css" />

    {{!
  <link rel="stylesheet" href="/css/templogin.css"> }}
  </head>

  <body {{! class="bg-dark text-light" }}>
    <div {{! class='flex-column justify-space-around' }}>
      <header
        class='navBar display-flex text-center justify-space-between align-center p-2'
      >
        {{! <header class="display-flex justify-space-between align-center p-2"> }}

        <h1 class=''>
          <span role='img' aria-label='book'>📖</span>
          <a class='text-light' href='/'>Bookbusta</a>
          <span role='img' aria-label='book'>📖</span>
        </h1>
        {{! <a href="/">Bookbusta</a>
        <span role="img" aria-label="book">📖</span> }}
        {{! </h1> }}
        {{!-- <nav>
          <a href="/searchbooks">Search Books</a> |
          <!-- Conditionally render login or logout links -->
          {{#if logged_in}}
          <a href="/profile">profile</a> |
          <button class="no-button" id="logout">logout</button>
          {{else}}
          <a href="/login">login</a>
          {{/if}}
        </nav> --}}

        <nav class='nav nav-tabs justify-content-center'>

          <li class='nav-item'>
            <a class='nav-link active' href='/reviews'><i
                class='fa fa-fw fa-book'
              ></i>Reviews</a>
          </li>
        
          <li class='nav-item'>
            <a class='nav-link active' href='/searchbooks'><i
                class='fa fa-fw fa-book'
              ></i>Search Books</a>
          </li>
          <li class='nav-item'>
            <a class='nav-link active' href='/questionaire'><i
                class='fa fa-fw fa-search'
              ></i>Search By Genre</a>
          </li>
          <li class='nav-item'>
            <a class='nav-link active' href='/profile'><i
                class='fa fa-fw fa-laptop'
              ></i>Profile</a>
          </li>
          <!-- Conditionally render login or logout links -->
          {{#if logged_in}}
            <li class='nav-item'>
              <button class=' nav-link active' id='logout'><i
                  class='fa fa-fw fa-user'
                ></i>Logout</button>
            </li>
          {{else}}
            <li class='nav-item'>
              <a class='nav-link active' href='/login'><i
                  class='fa fa-fw fa-user'
                ></i>Login</a>
            </li>
          {{/if}}
        </nav>

      </header>
      <main class='container container-fluid mt-5'>
        <!-- Render the sub layout -->
        {{{body}}}
      </main>
      <footer>

      </footer>
    </div>

    <!-- Render script for logged in users only -->
    {{#if logged_in}}
      <script src='/js/logout.js'></script>
    {{/if}}
  </body>

</html>